/*package ninja.kyle.projectchain;

import ninja.kyle.projectchain.exchanges.Exchange;
import ninja.kyle.projectchain.internallib.Pair;

public interface MarketOpIteration {

  //Returns amount of times we query the exchange.
  int getExchangeWeight();

  //Execute iteration will execute one iteration of the intended stratergy.
  //Perhaps returning early with True to signal that the process should terminate next
  //iteration.
  boolean executeIteration(Exchange exchange, Pair<Asset,Asset> market);
}*/
